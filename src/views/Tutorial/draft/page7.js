import React from "react";
import { Paragraph } from "../../../styled";

export default {
  Prose: () => (
    <Paragraph>
      <em>"Too plain! Add some bounce to my rotation.”</em>
    </Paragraph>
  )
};
