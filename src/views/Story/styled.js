import g from "glamorous";

// export const PreviewContainer = g.div({
//   position: "relative",
//   margin: `${9 / 16}rem 0`
// });

export const Pager = g.div({
  display: "flex",
  justifyContent: "space-between",
  alignItems: "center",
  margin: `${30 / 16}rem 0`
});

// export const StoryContainer = g.div({
//   display: "flex"
// });

export const Stage = g.div({
  display: "flex",
  justifyContent: "center",
  alignItems: "center",
  position: "relative",
  margin: `${30 / 16}rem 0`
});
